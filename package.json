{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "wrangler:dev": "pnpm build && wrangler dev",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "3.14.1592",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "wrangler": "^3.93.0"
  }
}
